<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Posts on Calvin-Khang Ta</title><link>https://tacalvin.github.io/posts/</link><description>Recent content in Posts on Calvin-Khang Ta</description><generator>Hugo -- gohugo.io</generator><language>en-us</language><copyright>&lt;a href="https://creativecommons.org/licenses/by-nc/4.0/" target="_blank" rel="noopener"&gt;CC BY-NC 4.0&lt;/a&gt;</copyright><lastBuildDate>Fri, 17 Jul 2026 17:29:22 -0700</lastBuildDate><atom:link href="https://tacalvin.github.io/posts/index.xml" rel="self" type="application/rss+xml"/><item><title>Mixcompress</title><link>https://tacalvin.github.io/posts/mixcompress/</link><pubDate>Fri, 17 Jul 2026 17:29:22 -0700</pubDate><guid>https://tacalvin.github.io/posts/mixcompress/</guid><description>&lt;h2 id="research-report-mixcompress-mixture-of-experts-for-variable-rate-learned-image-compression"&gt;Research Report: MixCompress: Mixture of Experts for Variable Rate Learned Image Compression&lt;/h2&gt;
&lt;h3 id="1-authors-and-institutions"&gt;1. Authors and Institution(s)&lt;/h3&gt;
&lt;p&gt;The research paper &amp;ldquo;MixCompress: Mixture of Experts for Variable Rate Learned Image Compression&amp;rdquo; was authored by Calvin-Khang Ta, Praneet Singh, Tong Shao, and Peng Yin. All authors are affiliated with Dolby Laboratories, USA.&lt;/p&gt;
&lt;h3 id="2-how-this-work-fits-into-the-broader-research-landscape"&gt;2. How This Work Fits into the Broader Research Landscape&lt;/h3&gt;
&lt;p&gt;Learned Image Compression (LIC) has emerged as an alternative to traditional image codecs by optimizing non-linear transforms and entropy models end-to-end for a rate-distortion objective. Early advancements in LIC involved hierarchical hyper-priors, joint autoregressive modeling, and refined context modeling. More recent developments have incorporated Transformer and linear-attention architectures, such as LALIC, LIC-TCM, and FAT, to enhance representational power and maintain practical decoding complexity, establishing LIC as a high-performance solution for image coding.&lt;/p&gt;</description><content type="html"><![CDATA[<h2 id="research-report-mixcompress-mixture-of-experts-for-variable-rate-learned-image-compression">Research Report: MixCompress: Mixture of Experts for Variable Rate Learned Image Compression</h2>
<h3 id="1-authors-and-institutions">1. Authors and Institution(s)</h3>
<p>The research paper &ldquo;MixCompress: Mixture of Experts for Variable Rate Learned Image Compression&rdquo; was authored by Calvin-Khang Ta, Praneet Singh, Tong Shao, and Peng Yin. All authors are affiliated with Dolby Laboratories, USA.</p>
<h3 id="2-how-this-work-fits-into-the-broader-research-landscape">2. How This Work Fits into the Broader Research Landscape</h3>
<p>Learned Image Compression (LIC) has emerged as an alternative to traditional image codecs by optimizing non-linear transforms and entropy models end-to-end for a rate-distortion objective. Early advancements in LIC involved hierarchical hyper-priors, joint autoregressive modeling, and refined context modeling. More recent developments have incorporated Transformer and linear-attention architectures, such as LALIC, LIC-TCM, and FAT, to enhance representational power and maintain practical decoding complexity, establishing LIC as a high-performance solution for image coding.</p>
<p>Despite these advances, a significant limitation of state-of-the-art LIC models is the requirement for separate, independently trained models for each specific rate-distortion operating point. This leads to increased training costs, substantial storage overhead for multiple checkpoints, and increased deployment complexity.</p>
<p>Variable Bit-Rate (VBR) LIC methods aim to address this limitation by enabling a single unified model to support multiple operating points. Existing VBR approaches typically condition a shared dense backbone using techniques such as latent gain scaling, global affine transformations, or conditional convolutions. While these methods simplify deployment, they often exhibit a performance gap compared to single-rate models optimized for specific bit-rates. This performance degradation is attributed to a fundamental optimization challenge: dense parameter modulation forces the shared backbone to approximate divergent rate-distortion mappings, leading to &ldquo;feature entanglement&rdquo; or &ldquo;gradient conflict.&rdquo; Specifically, the gradients required for low-rate smoothing can conflict with those for high-rate detail preservation, leading to suboptimal performance across the rate spectrum.</p>
<p>This work introduces a framework that integrates sparsely gated Mixture-of-Experts (MoE) architectures, previously demonstrated in large-scale natural language processing and vision models (e.g., GShard, Switch Transformer, Vision MoE), into the hierarchical transform-based LIC paradigm. MoE allows for conditional computation and structural specialization, offering a potential solution to the gradient conflict observed in dense VBR methods. The paper also builds upon the concept of auxiliary wavelet shortcuts (AuxT) which accelerate training by offloading feature decorrelation, extending it to a rate-aware variant. By combining structural specialization with dynamic capacity allocation and rate-adaptive feature modulation, MixCompress aims to overcome the limitations of current VBR LICs and establish a more efficient and higher-performing unified compression framework.</p>
<h3 id="3-key-objectives-and-motivation">3. Key Objectives and Motivation</h3>
<p>The central objective of this research is to develop a unified variable bit-rate (VBR) learned image compression (LIC) framework that can effectively support a wide range of rate-distortion operating points within a single model, without compromising performance compared to, or even surpassing, individually optimized single-rate models. This framework aims to significantly reduce the computational, storage, and deployment overhead associated with managing multiple single-rate LIC models.</p>
<p>The primary motivation stems from an identified limitation in existing VBR LIC methods:</p>
<ol>
<li><strong>Gradient Interference in Dense Parameter Modulation:</strong> Current VBR approaches rely on conditioning a dense, shared backbone (e.g., via conditional convolutions or latent gain scaling). The authors hypothesize that this forces the network parameters to approximate conflicting mappings across the rate-distortion curve. Specifically, optimizing for low bit-rates often involves smoothing high-frequency details to reduce entropy, while optimizing for high bit-rates requires preserving these details. This creates antagonistic gradients during training, where updates for one rate point can degrade performance for another. The cosine similarity between these divergent task gradients can be negative, leading to suboptimal feature learning and a &ldquo;capacity bottleneck.&rdquo;</li>
<li><strong>Lack of Structural Specialization:</strong> Most existing VBR methods adapt representations but do not alter the model&rsquo;s fundamental structure or capacity in a rate-dependent manner. This limits the model&rsquo;s ability to allocate computational resources or specialized processing pathways tailored to the distinct demands of different bit-rate regimes.</li>
<li><strong>High Overhead of Single-Rate Models:</strong> The practical deployment of high-performance LIC often necessitates training and storing multiple independent models, one for each desired rate point. This leads to substantial training costs, large storage footprints for checkpoints, and increased complexity in managing and deploying codecs across various quality settings.</li>
</ol>
<p>To address these motivations, the MixCompress framework aims to:</p>
<ul>
<li><strong>Mitigate Gradient Conflict:</strong> Introduce sparse structural specialization using Mixture-of-Experts (MoE) modules. This allows different &ldquo;experts&rdquo; or subnetworks to specialize in handling specific rate regimes, thereby isolating conflicting gradients and preventing destructive interference across the rate-distortion curve.</li>
<li><strong>Enable Dynamic Capacity Allocation:</strong> Extend the MoE concept to a Mixture-of-Depths (MoD) framework, which dynamically scales the model&rsquo;s representational capacity. This means that higher bit-rate settings, which typically require more complex feature representation, can be routed to &ldquo;deeper&rdquo; or more computationally intensive experts, while lower bit-rates utilize shallower, more efficient pathways.</li>
<li><strong>Introduce Rate-Aware Feature Energy Compaction:</strong> Implement Conditional Auxiliary Transforms (CAT), a rate-aware extension of wavelet shortcuts. CAT dynamically modulates sub-band energy based on the target bit-rate, allowing for explicit control over detail preservation versus smoothing, without altering the core entropy model. This complements the structural specialization by providing rate-dependent feature conditioning.</li>
<li><strong>Achieve Pareto-Optimal Performance and Efficiency:</strong> Develop a single unified model that can match or exceed the performance of multiple independently trained single-rate models, while simultaneously reducing the overall parameter count, training time, and storage footprint.</li>
</ul>
<h3 id="4-methodology-and-approach">4. Methodology and Approach</h3>
<p>MixCompress integrates three primary components: sparsely gated Mixture-of-Experts (MoE) or Mixture-of-Depths (MoD) modules, and Conditional Auxiliary Transforms (CAT), within a standard learned image compression (LIC) backbone. The architecture modifies existing LIC codecs by replacing bottleneck blocks in the analysis transform, synthesis transform, and hyper-prior networks with these rate-conditioned modules.</p>
<p><strong>4.1. Preliminaries: Variable Rate Learned Image Compression</strong>
The framework builds on the standard end-to-end optimized non-linear transform coding. An input image <code>x</code> is transformed into a latent representation <code>y</code> by an analysis transform <code>g_a</code>, and reconstructed as <code>x̂</code> by a synthesis transform <code>g_s</code>. A hyper-encoder <code>h_a</code> maps <code>y</code> to a hyper-latent <code>z</code>. Both <code>y</code> and <code>z</code> are quantized to <code>ŷ</code> and <code>ẑ</code>. The objective function for a single-rate model minimizes <code>L_λ(θ) = E[λD(x, x̂; θ) + R(ŷ; θ) + R(ẑ; θ)]</code> for a fixed Lagrange multiplier <code>λ</code>. For variable-rate control, the model is trained by sampling <code>λ</code> from a distribution <code>p(λ)</code> during training, optimizing <code>L_VBR(θ) = E_λ,x[λD(x, x̂; θ) + R(ŷ; θ) + R(ẑ; θ)]</code>. The authors articulate that dense parameter modulation (e.g., conditional convolutions or latent scaling) for adapting to varying <code>λ</code> leads to &ldquo;Gradient Interference,&rdquo; where gradients for low bit-rate smoothing conflict with those for high bit-rate detail preservation, resulting in suboptimal performance.</p>
<p><strong>4.2. Conditional Auxiliary Transforms (CAT)</strong>
To enhance feature energy compaction in a rate-aware manner, MixCompress introduces CAT, an extension of AuxT shortcuts.</p>
<ul>
<li><strong>Wavelet Decomposition:</strong> Given an intermediate feature map <code>P</code>, a 2D Haar discrete wavelet transform (DWT) decomposes it into four frequency sub-bands (<code>P_LL</code>, <code>P_LH</code>, <code>P_HL</code>, <code>P_HH</code>). These are then tokenized.</li>
<li><strong>Rate-Conditioned Subband-Aware Scaling:</strong> Each CAT block maintains learnable scaling vectors for each sub-band (<code>s_base</code>). A FiLM (Feature-wise Linear Modulation) head, conditioned on a learned embedding of the rate parameter <code>λ_e</code>, produces dynamic scaling parameters (<code>γ_λ</code>, <code>β_λ</code>). These parameters modulate <code>s_base</code> to create <code>s_enc_λ</code> for encoder-side scaling, which is applied token-wise to the wavelet features. A linear projection maps the scaled tokens to the shortcut output channels, which are then injected residually into the encoder.</li>
<li><strong>Decoder Symmetry (iCAT):</strong> An inverse CAT (iCAT) mirrors the encoder operations on the decoder side, applying reciprocal scaling based on a decoder FiLM head and using an inverse DWT for reconstruction.</li>
<li><strong>Benefit:</strong> CAT/iCAT provides rate-adaptive auxiliary pathways for energy compaction without altering the primary codec structure or entropy model, thereby allowing dynamic adjustment of detail preservation based on <code>λ</code>.</li>
</ul>
<p><strong>4.3. Mixture of Experts (MoE)</strong>
To address gradient interference, MixCompress incorporates sparsely gated MoE modules.</p>
<ul>
<li><strong>Sparse Structural Routing:</strong> Instead of dense modulation, MixCompress replaces bottleneck layers with MoE blocks. Given an input feature <code>f</code> and a rate embedding <code>λ_e</code>, a routing network (conditioned on <code>λ_e</code>) generates a probability distribution over <code>N</code> parallel expert networks (<code>E_1, ..., E_N</code>). A separate <code>E_global</code> expert always processes the input to ensure stable information flow. The block&rsquo;s output is the sum of <code>E_global</code> and the top-K selected experts (typically K=2).</li>
<li><strong>Gradient Conflict Mitigation:</strong> The key advantage is that the routing mechanism isolates gradient conflicts. The parameters of a MixCompress block are partitioned into shared (<code>θ_shared</code>) and routed (<code>Θ_routed</code>). While <code>θ_shared</code> still receives gradients from all operating points, the inner product of gradients for the <code>Θ_routed</code> subspace is bounded by the intersection of active experts for two different rate points (<code>T_low ∩ T_high</code>). By reducing the volume of parameters subject to conflicting updates, the overall interference is diminished, shifting gradient dynamics towards orthogonality.</li>
<li><strong>Expert Collapse Prevention:</strong> To prevent a small subset of experts from dominating, additive Gaussian noise (<code>ϵ</code>) is injected into the router logits during training. This noise is annealed linearly over epochs, encouraging exploration in early stages and stabilization later.</li>
<li><strong>Task Conditioned Experts &amp; Inference Efficiency:</strong> Since routing and CAT modulations depend only on <code>λ_e</code> (independent of the input image <code>x</code>), expert assignments and modulation parameters can be precomputed and cached in a lookup table. This introduces negligible routing overhead during inference.</li>
</ul>
<p><strong>4.4. Mixture of Depths (MoD) Extension</strong>
To dynamically scale representational capacity for higher bit-rates, a Mixture-of-Depths (MoD) variant is proposed.</p>
<ul>
<li><strong>Progressive Depth Experts:</strong> In MoD, experts have varying depths. The <code>i</code>-th expert is a composite function of <code>i</code> nested transformation blocks (e.g., Bi-RWKV blocks for LALIC or convolutional blocks for LIC-TCM). By routing high-rate samples to deeper experts, the model can expand its capacity when required, while lower-rate samples can use shallower, more efficient experts.</li>
</ul>
<p><strong>4.5. Experimental Setup</strong></p>
<ul>
<li><strong>Training Data:</strong> OpenImages (first 400K images), randomly cropped to 256x256 patches.</li>
<li><strong>Optimization:</strong> Adam, batch size 8.</li>
<li><strong>Rate-Distortion Optimization:</strong> MSE objective, with <code>λ ∈ {0.0018, 0.0035, 0.0067, 0.0130, 0.0250, 0.0483}</code>.</li>
<li><strong>Training Schedule:</strong> Unified training for all <code>λ</code> values (sampling one <code>λ</code> per mini-batch), for the <em>same total duration</em> as a single-rate model (40 epochs at 1e-4, 4 at 1e-5, 4 fine-tuning at 512x512 crops).</li>
<li><strong>Model Integration:</strong> Implemented on LALIC and LIC-TCM backbones, with MixCompress blocks replacing the last layer at bottlenecks of analysis, synthesis, and hyper-prior networks.</li>
<li><strong>Configuration:</strong> 32-dimensional <code>λ_e</code>, <code>N=4</code> experts, <code>K=2</code> active experts (plus one shared).</li>
</ul>
<h3 id="5-main-findings-and-results">5. Main Findings and Results</h3>
<p><strong>5.1. Rate-Distortion Performance (BD-Rate)</strong>
Extensive evaluations demonstrate that MixCompress variants achieve or surpass the performance of individually optimized single-rate baselines, while also outperforming existing dense-modulation variable bit-rate (VBR) methods.</p>
<ul>
<li><strong>Against Single-Rate Models:</strong> MixCompress-LALIC-MoD consistently achieves significant BD-rate reductions (PSNR) compared to the VTM-23.1 anchor. For example, it reaches -25.73% on CLIC Valid, -28.71% on CLIC Test, and -28.88% on Tecnick, outperforming the single-rate LALIC baseline (e.g., -26.03% on Tecnick) and other strong single-rate architectures like FAT and LIC-TCM. The homogeneous MixCompress-LALIC-MoE also improves upon single-rate LALIC on CLIC and Tecnick. This is achieved despite training a single unified model for the same total duration as one single-rate model.</li>
<li><strong>Against Variable-Rate Models:</strong> Re-implemented dense-modulation VBR strategies, LALIC-QRAF and LALIC-CondConv, exhibited substantial performance degradation, sacrificing 3.7 to 12.0 percentage points of BD-rate relative to single-rate LALIC. In contrast, MixCompress-LALIC-MoE effectively closes this variable-rate gap, and MixCompress-LALIC-MoD surpasses the single-rate counterpart.</li>
</ul>
<p><strong>5.2. Gradient Conflict Mitigation</strong>
Empirical validation of the gradient conflict hypothesis was conducted by analyzing layer-wise gradient cosine similarity between extreme rate points (<code>λ_low</code> and <code>λ_high</code>).</p>
<ul>
<li>Dense baselines (QRAF and CondConv) exhibited distributions with long negative tails, indicating severe gradient conflicts where optimization for one rate point detrimentally affects another.</li>
<li>MixCompress successfully shifted the gradient cosine similarity distribution towards zero, implying a reduction in destructive interference and a move towards orthogonal gradient updates. The percentage of encoder layers exhibiting negative gradient cosine similarity was significantly reduced from 78-79% in dense methods to 31% (MoE) and 22% (MoD) in MixCompress variants.</li>
</ul>
<p><strong>5.3. Visual Quality</strong>
Qualitative visual comparisons demonstrated that dense-modulation VBR methods, such as QRAF, produced over-smoothed reconstructions at high bit-rates, failing to preserve fine details (e.g., a thin cable in Figure 6). This over-smoothing is consistent with the gradient conflict where low bit-rate objectives influence high bit-rate reconstructions. MixCompress-MoE and MixCompress-MoD successfully preserved these high-frequency details at comparable bit-rates, indicating improved fidelity due to structurally isolated gradients.</p>
<p><strong>5.4. MoE vs. MoD</strong>
MoD variants consistently outperformed MoE across all benchmarks. This suggests that dynamically scaling representational capacity through depth-adaptive experts provides advantages beyond parameter-only specialization, better meeting the varying computational demands of different bit-rates.</p>
<p><strong>5.5. Latent Analysis and Capacity Allocation</strong></p>
<ul>
<li>Channel utilization heatmaps (Figure 7) showed that dense baselines like QRAF forced highly overlapping and entangled parameter usage across all rate points.</li>
<li>MixCompress, in contrast, dynamically reallocated capacity, sharing channels between adjacent rates for smooth transitions while decoupling parameters for low and high bit-rates, minimizing gradient conflict.</li>
<li>The severe under-performance of LALIC-QRAF was attributed to its latent scaling strategy being nullified by LayerNorm operations in the LALIC backbone, combined with unresolved gradient interference. MixCompress avoids these issues by routing latents through structurally distinct pathways.</li>
</ul>
<p><strong>5.6. Computational and Deployment Efficiency</strong>
MixCompress offers significant advantages in deployment efficiency:</p>
<ul>
<li>A single MixCompress-MoD model, the largest variant, uses 156 million total parameters to cover the entire rate-distortion curve, compared to approximately 397 million parameters required for six independently trained single-rate LALIC models.</li>
<li>Due to caching of precomputed expert assignments and modulation parameters (as they depend only on <code>λ_e</code>), the inference overhead is minimal. Encoding and decoding times show only slight increases compared to a single-rate LALIC model, with manageable memory usage.</li>
</ul>
<p><strong>5.7. Ablation Studies</strong>
Ablation studies confirmed the contribution of each component:</p>
<ul>
<li>Standard conditional convolutions (Cond-Conv) showed improvements over VTM-23.1 but underperformed single-rate baselines.</li>
<li>Adding static AuxT shortcuts partially mitigated this gap, and replacing them with rate-aware CAT transforms further improved performance.</li>
<li>Replacing Cond-Conv with MoE blocks narrowed the performance gap, and MoD blocks consistently yielded the strongest BD-Rate performance, surpassing single-rate baselines. This confirms that sparse expert specialization and depth-adaptive capacity allocation are more effective than dense weight modulation for VBR LIC.</li>
</ul>
<h3 id="6-significance-and-potential-impact">6. Significance and Potential Impact</h3>
<p>MixCompress presents a significant contribution to the field of learned image compression, particularly in the domain of variable bit-rate (VBR) coding. Its primary significance lies in addressing the long-standing challenge of achieving high-performance VBR image compression from a single unified model without the performance degradation typically associated with such approaches.</p>
<p>The potential impacts are multifaceted:</p>
<ol>
<li><strong>Advancement in VBR LIC Performance:</strong> By demonstrating that a single VBR model can not only match but often surpass the rate-distortion performance of individually optimized single-rate baselines, MixCompress establishes a new benchmark. This fundamentally challenges the previous understanding that a trade-off between variable-rate capability and peak performance was inevitable due to issues like gradient conflict.</li>
<li><strong>Overcoming Gradient Interference:</strong> The research empirically validates the concept of gradient interference in dense-modulation VBR methods and successfully introduces sparse structural specialization (MoE/MoD) as an effective mechanism for its mitigation. This provides both a practical solution and a theoretical insight into optimizing multi-objective learning problems in image compression.</li>
<li><strong>Enhanced Deployment Efficiency:</strong> MixCompress drastically reduces the practical overhead associated with deploying learned codecs. Replacing multiple single-rate checkpoints with a single unified model leads to substantial reductions in storage requirements, simplifies model management, and streamlines deployment pipelines in real-world applications. This also implies reduced training costs and computational resources.</li>
<li><strong>Flexible and Scalable Architectures:</strong> The integration of Mixture-of-Experts and Mixture-of-Depths provides a blueprint for building more flexible and scalable compression architectures. Dynamically allocated capacity based on rate demands allows for efficient resource utilization, enabling the model to adapt its complexity to encode images at various quality levels without sacrificing performance.</li>
<li><strong>New Pareto Frontier:</strong> The authors claim to establish a new Pareto frontier for practical neural image compression. This implies achieving superior rate-distortion performance and representational density from a single model, with minimal additional inference cost, compared to prior methods. This could accelerate the adoption of learned image compression in commercial products and services requiring adaptive quality control.</li>
</ol>
<p>In conclusion, MixCompress offers a robust, efficient, and high-performing solution for variable bit-rate learned image compression, addressing critical limitations of prior work and paving the way for more practical and adaptable neural codecs.</p>
]]></content></item></channel></rss>